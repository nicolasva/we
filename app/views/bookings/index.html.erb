<h1>Bookings list</h1>
<ul>
  <% @bookings.each do |booking| %>
    <li>
      <%= link_to booking.name, booking_path(booking.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "Demander une nouvelle réservation", new_booking_path %>
